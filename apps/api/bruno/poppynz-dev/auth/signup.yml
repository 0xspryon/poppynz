info:
  name: signup
  type: http
  seq: 1

http:
  method: POST
  url: http://localhost:3000/app/api/v1/auth/sign-up
  body:
    type: json
    data: |-
      {
        "email": "test@gmail.com",
        "role": "service-provider"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
